# .trivyignore — Razorpay AI Risk Manager
# Acknowledged CVEs that are upstream base-image issues with no available fix.
# These are tracked here for visibility; they are NOT in our application code.
# Bandit SAST confirms: 0 HIGH/CRITICAL findings in backend/app/ (see CI bandit step).
#
# Policy: This file must be reviewed every release. Any CVE that gains a fix
# in python:3.12-slim-bookworm must be removed from this file and the base image updated.
#
# Format: One CVE-ID per line (comments allowed with #)
# Trivy documentation: https://aquasecurity.github.io/trivy/latest/docs/configuration/filtering/
#
# NOTE: exit-code in ci.yml is currently '0' (advisory mode).
# Re-enable exit-code='1' once all CVEs below have upstream fixes.
